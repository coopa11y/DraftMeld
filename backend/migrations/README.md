# Database migrations

Versioned SQLite migrations will live here when persistence enters the first feature milestone. Migrations must be forward-only in released versions and covered by upgrade tests.
